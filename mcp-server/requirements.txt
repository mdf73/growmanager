mcp>=1.2.0,<2.0.0
httpx>=0.27.0,<1.0.0
# uvicorn n'est utile qu'en mode GROWMANAGER_MCP_TRANSPORT=http (déploiement Linux/Docker) ;
# inoffensif à installer sur le poste Windows en usage stdio.
uvicorn>=0.29.0,<1.0.0
