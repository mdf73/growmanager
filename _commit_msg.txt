feat(rosin): édition d'extraction + maillage obligatoire

- Maillage rendu obligatoire à la création et à l'édition (schémas, garde backend, select required + validation front)
- Nouvel endpoint PUT /api/rosin/{id} : édite tous les paramètres sans re-déduire les stocks sources, et synchronise le stock Rosin produit (quantité + maillage)
- Colonne RosinExtraction.id_stock_produit pour lier l'extraction au stock produit (migration auto via run_migrations, pas de SQL manuel)
- Front : EditExtractionModal.tsx + bouton crayon sur chaque ligne + rosinAPI.update()
- Wiki mis à jour (api/stock-extractions, database/stock, log)
