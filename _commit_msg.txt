feat(mobile): Sprint A3 — URL serveur configurable + PWA installable

- api/client.ts : baseURL dynamique via localStorage (gm_server_url), helpers getServerUrl/setServerUrl/apiBaseURL/serverFileURL/testServerConnection — comportement web inchangé par défaut
- Parametrage (onglet Général) : section "Serveur (app mobile)" avec test de connexion /health et reload auto
- PWA : manifest.webmanifest + icônes 192/512/maskable générées depuis IconSeul.png + viewport-fit=cover
- Wiki : roadmap A3 validé + log
