docs(mobile): fixes /health + 100dvh validés — wiki log + règle de mise à jour APK

- wiki/log.md : entrée bugfixes mobile (proxy /health partout + h-screen-safe 100dvh)
- wiki/features/mobile-app.md : section "Mises à jour de l'app" (backend = rien, UI = rebuild APK)
