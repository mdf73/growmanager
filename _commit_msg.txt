feat: courbes capteurs T°/Hum/VPD dans les calendriers + export PDF

SensorDayChart.tsx (nouveau composant)
- Moyennes horaires (1 point/heure) calculées côté frontend
- 1 ligne par capteur (id_device) avec couleur dédiée + légende
- Tiles résumé min/moy/max · 3 courbes empilées (temp / hum / vpd)
- Props : date (ISO locale) + idEspace optionnel
- API : GET /api/temperature-logs?date_debut=T00:00:00&date_fin=T23:59:59

Intégration calendriers
- CalendrierGlobal — DayModal : SensorDayChart sous les events (toutes tentes)
- CalendrierCulture — panneau jour sélectionné : filtré sur id_espace

Export PDF CalendrierGlobal
- buildSensorSVGCharts() : SVG inline par page jour (T° / Hum / VPD)
- 1 polyline par capteur, moyennes horaires, légende si multi-capteurs
- Fetch logs en parallèle des events (Promise.all) dans ExportPDFModal
- CSS dédié .sensor-section dans le template HTML

Wiki : roadmap + frontend/pages + features/sensor-integration mis à jour
