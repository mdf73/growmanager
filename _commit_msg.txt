feat(F8): galerie photos — plantes & cultures

Backend
- Modèle SQLAlchemy `Photo` (id_plant/id_culture FK nullable, filepath,
  thumbnail_path, date_prise, notes, taille_ko, largeur_px, hauteur_px)
  → table créée via create_all au démarrage
- Router `photos.py` : POST /api/photos/upload (multipart, Pillow
  compression JPEG + thumbnail 300×300 crop centré, max 2 Mo, exif_transpose),
  GET /api/photos/?id_culture=&id_plant=, DELETE /api/photos/{id}
- StaticFiles mount `/uploads` → `/app/uploads` (volume Docker persistant)
- Pillow==10.3.0 ajouté dans requirements.txt
- Import + include_router dans main.py

Frontend
- `api/photos.ts` : interface Photo, photoUrl(), photosAPI.list/upload/delete
- `components/culture/PhotoGallery.tsx` :
    • drag-drop + clic pour upload (multi-fichiers)
    • grille responsive (2→4 colonnes) avec thumbnails lazy-load
    • lightbox plein écran : nav prev/next, clavier ��/→/Esc, note + date,
      bouton suppression
    • note optionnelle pré-remplie avant upload
- `Culture.tsx` :
    • onglet "Photos (N)" (Camera icon) dans la fiche culture
    • badge photo count (🎞 N) sur chaque card de la liste
    • query photos-count partagée entre card et detail view

Wiki mis à jour : roadmap, database/schema-overview, api/overview
