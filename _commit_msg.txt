feat: multi-sources en entrée pour extractions Rosin et Hash

- Formulaires NouvelleExtractionModal + NouvelleHashModal : section
  dynamique permettant de sélectionner N stocks/variétés en entrée
  (chaque ligne : dropdown stock + quantité g + bouton supprimer)
- Backend : helpers _deduct_sources / _apply_deductions dans extractions.py,
  déduction multi-stock avec fermeture auto si stock épuisé
- Schémas Pydantic : ExtractionSource + champ sources: Optional[list]
  sur RosinExtractionBase et HashExtractionBase
- DB : colonne sources JSON NULL sur RosinExtraction et HashExtraction
  (migration backend/migrate_sources.sql — déjà appliquée)
- Frontend api/stock.ts : interface ExtractionSource + sources? sur
  RosinExtraction et HashExtraction
- Rétrocompat : id_stock_source conservé (= sources[0]) pour les vues existantes
- Wiki mis à jour : wiki/api/stock-extractions.md
