fix: audit complet erreurs backend + frontend — 0 erreur py_compile + tsc

Backend Python :
- cultures.py : get_stats() tronqué, return dict restauré
- sechage.py : corps if not ps vide, raise HTTPException 404 remis

Frontend TypeScript — bugs sérieux :
- api/suiviSolVivant.ts : SuiviSolVivantCreate Omit fix (5 sous-tableaux)
- api/espaces.ts : EspaceCultureCreate Omit fix (equipements)
- api/stock.ts : champs nullables passés en string|null / number|null
- api/materiel.ts : champ date_sortie_stock ajouté
- NouvellerCultureModal : b.id_parametre + 3 useState orphelins supprimés
- ClassementVarietes : NoteSlider.value accepte number|null|undefined
- EmptyState : icon prop LucideIcon → React.ElementType
- Graines : prop title illégale sur Lucide → span wrapper
- Stock + Statistiques : null → ?? undefined pour fonctions date

Frontend TypeScript — cosmétiques (TS6133) :
- Imports inutilisés supprimés dans ~15 fichiers
- deleteGraine mutation morte, coutParLitre, fmt, ARROSAGE_COLORS supprimés

React Router :
- App.tsx : future flags v7_startTransition + v7_relativeSplatPath ajoutés
