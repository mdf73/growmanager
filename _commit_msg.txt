fix: add --skip-ssl to mysql restore command

The backup/restore endpoint was failing with:
  ERROR 2026 (HY000): TLS/SSL error: self-signed certificate in certificate chain

mysqldump already had --skip-ssl but the mysql client for restore did not.
Added --skip-ssl to the subprocess call in backup_restore() to match.

File: backend/app/routers/import_export.py
