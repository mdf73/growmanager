feat: Feature 3 — Docker multi-plateforme (production)

- backend/Dockerfile.prod : uvicorn 2 workers, sans --reload, mkdir uploads/
- frontend/Dockerfile.prod : multi-stage node:20 build + nginx:alpine serve
- frontend/nginx.prod.conf : SPA routing, proxy /api/, gzip, cache assets
- docker-compose.server.yml : 3 services prod (db fermé, backend, frontend:80)
- .env.production : template variables production
- wiki/architecture/deploiement.md : guide Linux + Windows, sauvegardes, HTTPS
