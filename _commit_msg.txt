feat: multi-clones — prélever N boutures en une fois

- API: endpoint clone accepte `quantite` (1-50), crée N plants indépendants
  numérotés automatiquement (#1, #2...) avec id_plant_mere commun
- Frontend: ClonageModal — sélecteur +/- quantité (max 20),
  label adaptatif, panel succès affiche toutes les boutures créées,
  actions "toutes enracinées" / "toutes ratées" en bulk
