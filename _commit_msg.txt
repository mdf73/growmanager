feat(calendrier): photo lightbox + navigation arrière DayModal→EventDrawer

- EventDrawer : bouton ← retour vers DayModal quand ouvert depuis la vue journée
  (× ferme tout, ← revient à la journée sans perdre le contexte)
- EventDrawer : section photos pour type_action='photo'
  · Charge toutes les photos de la culture via GET /api/photos/?id_culture=
  · Filtre par date_prise = date de l'event
  · Fallback ⚠️ si aucune correspondance de date (décalage date_prise/date_action)
  · Lightbox plein écran au clic, grille 2 colonnes, overlay note au hover
- ACTION_META : ajout type 'photo' → emoji 📷 label 'Photo'
- Backend photos.py : filtre optionnel par date (param ?date=YYYY-MM-DD)
  + parsing date_prise plus robuste (log stderr si format invalide)
- Wiki frontend/pages.md mis à jour
