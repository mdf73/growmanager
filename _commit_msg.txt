fix: race condition création variété dans NouveauPackModal

La variété était bien créée en DB mais le select affichait
"— Sélectionner —" car invalidateQueries est async : le cache
n'était pas encore mis à jour quand le select redevenait visible.

- addVariete, addBreeder, addFournisseur : invalidateQueries → setQueryData
  (mise à jour immédiate du cache, select correct instantanément)
- Ajout affichage erreur si addVariete.isError

Fichier : frontend/src/components/NouveauPackModal.tsx
Wiki : wiki/log.md
